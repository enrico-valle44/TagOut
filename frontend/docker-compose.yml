services:
  frontend:
    build: .
    #image: animal-report
    container_name: frontend
    ports:
      - "8080:80"
    restart: unless-stopped

  # backend:
  #   build:
  #   image: reports-aopi
  #   ports:
  #     - "27017:27017"
  #   volumes:
  #     - mongo-data:/data/db
  #   restart: unless-stopped
